[project]
name = "mcp-hello"
version = "0.1.0"
description = "Hello-world MCP server — template for all other services in mcp-services."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "tina4-python>=3.11.12",
    "pyjwt[crypto]>=2.10.0",
    "requests>=2.32.0",
]
